# 08 — 复核：确认、修改、标记已复核

**What to build:** **报价员**在并排界面上逐项确认或就地修改提取值，处理完所有待确认项后把这张**零件图**标记为"已复核"，使它成为可追溯的报价依据。

核心是**高风险字段强制复核规则**（ADR-0002）：尺寸与公差这类错读代价最高的字段，**无论图纸质量分级如何**一律标为"需确认"，必须人工过目。低风险字段（图号、零件名称、材料、数量）在"清晰"图上不强制逐个点确认，让复核的注意力落在真正危险的地方。

这条规则堵的是"清晰图上个别错读公差"这个致命缝隙——分级为"清晰"最容易让人放松警惕，而一个错读的公差足以让报价亏本。因此**分级永远不影响高风险字段**，判定表是领域层的静态常量，不是模型输出。

**这条不变量只能活在后端。** 前端不得自行判断某个字段要不要标"需确认"，否则规则就分裂到两个库里、缝 1 再也覆盖不住它。

**Blocked by:** 06

**Status:** claimed

- [x] 尺寸类与公差类字段一律标为"需确认"，在"清晰""一般""差"三档分级下行为完全一致
- [x] 低风险字段在"清晰"图上不强制逐项确认
- [x] **报价员**能逐项点击确认
- [x] **报价员**能就地修改提取值，无需跳转页面
- [x] 界面显示还剩几项待处理
- [x] 修改自动保存，刷新或断网后已做的修改仍在
- [x] 存在未处理的"需确认"项时，标记"已复核"被拒绝，并说明还差哪些
- [x] 全部处理完后可标记"已复核"，状态机迁移到"已复核"并写入带时间戳的事件
- [x] 高风险字段判定表是领域层静态常量，前端不参与该判断
- [x] 缝 1 有测试证明：清晰图上尺寸与公差仍需确认、有待确认项时无法标记已复核

## Comments

实现已落在 PR：https://github.com/qqzhangyanhua/zol-cad/pull/8

设计要点：

- 高风险判定表是领域层静态常量 `FIELD_RISK_CLASS_BY_KEY`：尺寸类 / 公差类一律需确认，分级只影响低风险字段。`requires_confirmation` 由后端算好再返回，前端只展示。
- 逐项确认与就地修改走用例层；第一次操作把状态从已提取迁到复核中。修改立即落库，刷新后仍在。编辑一项即视为已处理。
- 存在未处理需确认项时，标记已复核返回 409，文案列出还差哪些。全部处理后迁到已复核并写带时间戳的事件。
- 不做票 09（忽略 / 补录 / 重开 / 风险重算）和票 10（修正记录）。
- 缝 1：70 passed。前端 tsc + lint 通过。浏览器已走通：清晰图上标题栏不强制确认、尺寸公差需确认、未完成时标记已复核被拦、就地改最严公差为 IT6 后刷新仍在、全部确认后可标记已复核。
