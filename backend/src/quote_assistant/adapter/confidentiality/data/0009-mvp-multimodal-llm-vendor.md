# MVP 多模态大模型供应商（模板 · 决定未关闭）

**状态：blocked — 不得把本文件读成「已选定某供应商」。**

票 02 要求：只有当某一家的三条硬门槛都有**可引用的书面条款或书面回函**时，才允许在本 ADR 里**点名选定**。2026-08-25 的公开条款核验结论是：

- 识别效果对比 **blocked**（票 01 真实零件图样本未入库）；
- 可用的出筛集合为空（见下）；
- 因此**不填写「选定谁」**。

调研产出：`.scratch/mvp-quote-assistant/research/02-llm-vendor-compliance/`

本文件只固定**将来关闭决定时必须写进 ADR 的栏目**。未关闭前，下面带「待填」的格子保持原样。

## 决定（待填）

MVP 生产环境的提取引擎（ADR-0003 的可替换 Port）选用：

**待填：供应商 / 产品 / 型号 / 中国站地域 / SKU（必须是按量或已核验的不训练套餐）**

不得在证据齐之前把任何候选抄进这一行。

## 三条门槛各自的证据（待填）

关闭本 ADR 时，三行都必须是「通过」或「有条件通过且条件已写入实现约束」，每行带 URL + 引文或书面回函编号。

| 门槛 | 判定 | 证据（URL + 引文或回函） |
|------|------|--------------------------|
| 数据存储/处理在中国大陆 | 待填 | 待填 |
| 书面承诺不用于模型训练 | 待填 | 待填 |
| 可签 DPA（中国站主体、覆盖该推理 API） | 待填 | 待填 |

实现约束（有条件通过时必填，否则写「无」）：

- 锁定地域 / endpoint：待填
- 禁止使用的 SKU（如 Coding Plan、个人版、境外地域）：待填
- 禁止签署的除外协议（如数据授权用于训练）：待填

## 被排除者与排除原因

完整名单与引文见调研 `03-exclusions.md`。关闭本 ADR 时把当时仍有效的排除项抄到下表，避免口头重提。

| 供应商 | 排除类型 | 原因（指向条款） |
|--------|----------|------------------|
| 待关闭时从 03 转录 | 门槛排除 / 使用限制排除 | 待填 |

2026-08-25 快照（**不是**选定，只是调研当时的排除，供对照）：

- 智谱默认 API、DeepSeek 开放平台、讯飞星火、MiniMax：G2 未过（条款授权训练或匿名化训练）。
- Kimi 中国站开放平台：G2 帮助页与服务协议冲突，不能当通过。
- 阿里云百炼、腾讯云混元、火山方舟、百度千帆：G3 公开文本不足（国际站 DPA / 其他产品 DPA 不算）；火山、千帆的 G1 亦不足。
- 华为云盘古：G1/G2/G3 有公开书面文本，但服务协议 2.1(8) 禁止输入商业秘密 / 具有商业权益的数据，与 ADR-0005 的零件图场景冲突，使用限制排除。

## 识别效果（待填）

对比表：`.scratch/mvp-quote-assistant/research/02-llm-vendor-compliance/05-recognition-comparison.md`

关闭本 ADR 前必须：只在出筛集合内、只用票 01 真实样本与人工标注，覆盖标题栏、关键尺寸与公差、技术要求。禁止用虚构准确率关闭决定。

## 成本与区域（待填）

公开刊例与公式见 `04-cost-and-regions.md`。关闭时补：

- 选定型号的刊例价与测算日；
- 用票 01 样本测得的单张 token / 费用（不是公式示意）；
- OSS 地域与模型地域（spec：尽量同一境内区域；若只能做到「都不出境、但跨云」，写明接受的解释成本）。

## 与既有 ADR 的关系

- 不改变 ADR-0003（买通用多模态，不自建）和 ADR-0005（境内 + 不训练 + 可签 DPA，先于识别效果）。
- 本 ADR 只回答「买哪一家的哪一档」。未关闭前，spec 里「多模态大模型：未定」仍然成立；主干开发继续用假引擎（票 03–16），真实引擎接入（票 17）继续 blocked on 02。

## 关闭检查单

- [ ] 出筛集合非空，且选定对象在集合内
- [ ] 三门槛每条都有书面证据，不是销售口述
- [ ] 票 01 样本已用于 `05`，三组维度有真实对错统计
- [ ] 实现约束（地域、SKU、除外协议）写进本页
- [ ] 排除表已从 `03` 转录
- [ ] 本页标题去掉「模板 · 决定未关闭」，本段状态改为已决定
